# Core dependencies for Security Monitor System

# Computer Vision and AI
ultralytics>=8.0.0
opencv-python>=4.5.0
face-recognition>=1.3.0
mediapipe>=0.8.0
numpy>=1.21.0

# Audio processing
pygame>=2.0.0

# Configuration and utilities
PyYAML>=6.0
python-dotenv>=0.19.0

# Optional: For better performance
# torch>=1.9.0  # Uncomment if you want to specify PyTorch version
# torchvision>=0.10.0

# Development dependencies (optional)
# pytest>=6.0.0
# pytest-cov>=2.10.0
# black>=21.0.0
# flake8>=3.8.0
# mypy>=0.812

# Email support (included in standard library)
# smtplib - built-in
# email - built-in

# Threading and async (included in standard library)
# threading - built-in
# queue - built-in
# time - built-in

# System dependencies that may need to be installed separately:
# For face_recognition on some systems:
# - cmake
# - dlib
# - boost-python

# For audio on Linux:
# - libasound2-dev
# - libportaudio2
# - libportaudiocpp0

# Installation notes:
# 1. On Windows: All packages should install via pip
# 2. On macOS: May need to install cmake via brew first
# 3. On Linux: May need system packages for audio and face_recognition
#
# Linux installation example:
# sudo apt-get update
# sudo apt-get install cmake libopenblas-dev liblapack-dev
# sudo apt-get install libasound2-dev libportaudio2 libportaudiocpp0
# pip install -r requirements.txt
